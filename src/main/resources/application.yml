spring:
  application:
    name: management

  kafka:
    bootstrapServers: kafka:9092
    consumer:
      topics:
        client: client
        courier: courier
        coords: coords
        chat: chat
      group-id: group1
      auto-offset-reset: earliest